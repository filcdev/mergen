name: Release

on:
  release:
    types: [released]     # published release; skips drafts/prereleases
  workflow_dispatch:      # manual test run; performs a full release
    inputs:
      tag:
        description: Git tag to build and ship (e.g. v1.2.3). Optional; defaults to the release tag when triggered by a release.
        required: false
        type: string

permissions:
  contents: read

jobs:
  release-ios:
    name: iOS → TestFlight
    if: ${{ !github.event.release.prerelease }}
    runs-on: macos-15
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set up Java
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4.9.1
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Ruby
        uses: ruby/setup-ruby@e8944e80fb94b20106697132f8c20c665fab29e9 # v1.325.0
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Resolve version
        env:
          RAW_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.event.release.tag_name }}
        run: |
          [[ "$RAW_TAG" =~ ^v?[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "::error::invalid release tag: $RAW_TAG"; exit 1; }
          echo "VERSION=${RAW_TAG#v}" >> "$GITHUB_ENV"
          echo "Resolved version: ${RAW_TAG#v}"

      - name: Write App Store Connect API key
        env:
          ASC_KEY_P8: ${{ secrets.ASC_KEY_P8 }}
        run: |
          mkdir -p "${RUNNER_TEMP}/asc"
          printf '%s' "${ASC_KEY_P8}" > "${RUNNER_TEMP}/asc/AuthKey.p8"
          chmod 600 "${RUNNER_TEMP}/asc/AuthKey.p8"

      - name: Set up SSH for match
        env:
          MATCH_SSH_KEY: ${{ secrets.MATCH_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${MATCH_SSH_KEY}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Build and upload to TestFlight
        env:
          ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
          ASC_KEY_PATH: ${{ runner.temp }}/asc/AuthKey.p8
          MATCH_REPO_URL: ${{ secrets.MATCH_REPO_URL }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
        run: |
          bundle exec fastlane ios release version:"${VERSION}" asc_key_id:"${ASC_KEY_ID}" asc_issuer:"${ASC_ISSUER_ID}" asc_key_path:"${ASC_KEY_PATH}"

  release-android:
    name: Android → Google Play (internal)
    if: ${{ !github.event.release.prerelease }}
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Set up Java
        uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4.9.1
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Ruby
        uses: ruby/setup-ruby@e8944e80fb94b20106697132f8c20c665fab29e9 # v1.325.0
        with:
          ruby-version: "3.3"
          bundler-cache: true

      - name: Make Gradle executable
        run: chmod +x ./gradlew

      - name: Resolve version
        env:
          RAW_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.event.release.tag_name }}
        run: |
          [[ "$RAW_TAG" =~ ^v?[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "::error::invalid release tag: $RAW_TAG"; exit 1; }
          echo "VERSION=${RAW_TAG#v}" >> "$GITHUB_ENV"
          echo "Resolved version: ${RAW_TAG#v}"

      - name: Decode Android upload keystore
        env:
          ANDROID_UPLOAD_KEYSTORE_BASE64: ${{ secrets.ANDROID_UPLOAD_KEYSTORE_BASE64 }}
        run: |
          printf '%s' "${ANDROID_UPLOAD_KEYSTORE_BASE64}" | base64 --decode > "${RUNNER_TEMP}/mergen-upload.jks"
          chmod 600 "${RUNNER_TEMP}/mergen-upload.jks"
          if [[ ! -s "${RUNNER_TEMP}/mergen-upload.jks" ]]; then
            echo "::error::Decoded upload keystore is empty."
            exit 1
          fi

      - name: Write Play service account JSON
        env:
          PLAY_SERVICE_ACCOUNT_JSON: ${{ secrets.PLAY_SERVICE_ACCOUNT_JSON }}
        run: |
          printf '%s' "${PLAY_SERVICE_ACCOUNT_JSON}" > "${RUNNER_TEMP}/play-key.json"
          chmod 600 "${RUNNER_TEMP}/play-key.json"
          if [[ ! -s "${RUNNER_TEMP}/play-key.json" ]]; then
            echo "::error::Play service account JSON is empty."
            exit 1
          fi

      - name: Build and upload to Google Play
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/mergen-upload.jks
          ANDROID_STORE_PASS: ${{ secrets.ANDROID_UPLOAD_STORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_UPLOAD_KEY_ALIAS }}
          ANDROID_KEY_PASS: ${{ secrets.ANDROID_UPLOAD_KEY_PASSWORD }}
          PLAY_JSON_KEY_PATH: ${{ runner.temp }}/play-key.json
        run: |
          bundle exec fastlane android release
